

export interface theater {
    id:string,
    tid:string,
    name:string,
    latitude:number,
    longitude:number,
    hasShowtimes:string, // true or false?
    hasReservedSeating:boolean,
    isTicketing:boolean,
    distance:number
}