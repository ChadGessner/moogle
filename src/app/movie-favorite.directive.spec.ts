import { MovieFavoriteDirective } from './movie-favorite.directive';

describe('MovieFavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
